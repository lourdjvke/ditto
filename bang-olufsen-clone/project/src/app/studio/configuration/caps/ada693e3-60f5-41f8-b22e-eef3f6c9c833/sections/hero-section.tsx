/** Hero section — the page's lead block. */
export default function HeroSection() {
  return (
    <div className="h-full block absolute top-0 inset-x-0 z-300 bg-clr-6 [backdrop-filter:blur(10px)] 2xl:hidden" data-cid="n94" aria-hidden="true" />
  );
}
